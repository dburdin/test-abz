export const BASE_URL = "https://frontend-test-assignment-api.abz.agency/api/v1/";

const endpoints = {
  USERS_ENDPOINT: "users",
  POSITIONS_ENDPOINT: "positions",
  TOKEN_ENDPOINT: "token",
};

export const LENGTH_TO_TRIM = 25;

export const { USERS_ENDPOINT, POSITIONS_ENDPOINT, TOKEN_ENDPOINT } = endpoints;
